// npm install --save React-Router-Dom
    // essential components:
        // - BrowswerRouter - App Wrapper, teaches app how to use react-BrowswerRouter
        // - Switch - wraps <Route/> components to 'switch' the user's view
        // - Route - define the 'endpoint' (url directory)
        // - link - sends out a signal to <switch>, switch loops over it's routs to find matching endpoint 