import React from 'react'
import { connect } from 'react-redux'

const Tab = () => {
    return (
        <div>
            
           
        </div>
    )
}

// export default Tab;
export default connect(state=>state, {})(Tab);
